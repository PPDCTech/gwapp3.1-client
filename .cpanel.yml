---
deployment:
  tasks:
    - export REPO_PATH=/home/ppdcorg/repositories/gwapp3
    - export DEPLOYPATH=/public_html/gwapp3-1
    - cd $REPO_PATH
    - npm install
    - npm run build
    - /bin/cp -R $REPO_PATH/build/* $DEPLOYPATH
